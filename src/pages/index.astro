---
import BaseLayout from '../layouts/BaseLayout.astro'
import HeroVideo from '../components/HeroVideo.astro'
import portada from '../assets/hero-poster.jpg'
const TITLE = 'Arquimétrica — La pieza clave en su proyecto'
const DESC = 'Gestión integral de proyectos con visión estratégica, eficiencia y resultados de alto impacto.'
---
<BaseLayout title={TITLE} description={DESC} image={portada.src} canonical="https://arquimetrica.cr/">
<HeroVideo id="1hLBCOlptq8" start={7} end={74} headline="La pieza clave en su proyecto" sub="Gestión integral de proyectos con visión estratégica, eficiencia y resultados de alto impacto." />


<!-- Intro corta -->
<section class="container-px py-16">
  <div class="grid md:grid-cols-2 gap-10 items-center">
    <div>
      <h2 class="text-3xl font-bold text-[var(--navy)] leading-tight">Gestión integral de proyectos</h2>
      <p class="mt-4 text-lg leading-relaxed text-slate-700">
        Nos distinguimos por un servicio con enfoque estratégico, visión de negocio y excelencia en cada etapa del proyecto. Aplicamos mejores prácticas para lograr eficiencia, optimización de recursos y resultados de alto impacto.
      </p>
     <div class="mt-6 flex flex-wrap gap-4">
  <!-- Botón Principal (CTA) -->
  <a 
    href="/servicios" 
    class="group relative inline-flex items-center justify-center rounded-xl bg-[var(--azul)] px-6 py-3 text-sm font-semibold text-white transition-all duration-300 ease-out hover:bg-[var(--azul-oscuro)] hover:shadow-xl focus:outline-none focus-visible:ring-2 focus-visible:ring-[var(--azul)] focus-visible:ring-offset-2"
  >
    <span class="relative z-10 flex items-center gap-2">
      Ver servicios
      <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 transition-transform duration-300 group-hover:translate-x-1" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
      </svg>
    </span>
    <div class="absolute inset-0 -z-10 scale-95 rounded-xl bg-gradient-to-r from-[var(--azul)] to-[var(--azul-oscuro)] opacity-0 transition-all duration-300 group-hover:scale-100 group-hover:opacity-100"></div>
  </a>

  <!-- Botón Secundario (Link elegante) -->
  <a 
    href="/proyectos" 
    class="group inline-flex items-center justify-center rounded-xl border border-slate-300 bg-white px-6 py-3 text-sm font-medium text-slate-700 transition-all duration-300 hover:border-[var(--azul)] hover:bg-[var(--azul)]/5 hover:text-[var(--azul)] focus:outline-none focus-visible:ring-2 focus-visible:ring-[var(--azul)] focus-visible:ring-offset-2"
  >
    Explorar proyectos
    <svg xmlns="http://www.w3.org/2000/svg" class="ml-2 h-4 w-4 transition-transform duration-300 group-hover:translate-x-0.5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6" />
    </svg>
  </a>
</div>
    </div>

<div class="grid grid-cols-1 sm:grid-cols-2 gap-6 md:gap-8">
  {["1", "2", "3", "4"].map((num) => (
    <div 
      key={num} 
      class="relative aspect-video rounded-xl overflow-hidden shadow-md group"
    >
      <img 
        src={`/images/gestion-${num}.webp`} 
        alt={`Proyecto de gestión ${num}`} 
        class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-105"
      />
      <div class="absolute inset-0 bg-gradient-to-t from-black/50 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
    </div>
  ))}
</div>
  </div>
</section>



<!-- Servicios destacados -->
<!-- Servicios destacados -->
<section class="bg-[var(--light)] py-16">
  <div class="container-px">
    <h2 class="text-3xl font-bold text-[var(--navy)] text-center">Servicios</h2>
    <p class="mt-3 text-slate-700 max-w-3xl mx-auto text-center">
      Ofrecemos proceso integral llave en mano — del papel a la realidad.
    </p>

    <!-- Proceso visual: planos → obra → terminado -->
    <div class="mt-12 flex flex-col md:flex-row items-center justify-center gap-8 md:gap-12">
      <div class="relative w-64 h-64 rounded-xl overflow-hidden shadow-lg border-2 border-[var(--azul)]">
        <img 
          src="/images/servicios-planos.jpg" 
          alt="Planos arquitectónicos" 
          class="w-full h-full object-cover"
        />
      </div>
      <div class="flex items-center justify-center">
        <div class="flex space-x-2">
          <div class="w-3 h-3 bg-[var(--azul)] rounded-full"></div>
          <div class="w-3 h-3 bg-[var(--azul)] rounded-full"></div>
          <div class="w-3 h-3 bg-[var(--azul)] rounded-full"></div>
        </div>
      </div>
      <div class="relative w-64 h-64 rounded-xl overflow-hidden shadow-lg border-2 border-[var(--azul)]">
        <img 
          src="/images/servicios-construccion.jpg" 
          alt="Obra en construcción" 
          class="w-full h-full object-cover"
        />
      </div>
      <div class="flex items-center justify-center">
        <div class="flex space-x-2">
          <div class="w-3 h-3 bg-[var(--azul)] rounded-full"></div>
          <div class="w-3 h-3 bg-[var(--azul)] rounded-full"></div>
          <div class="w-3 h-3 bg-[var(--azul)] rounded-full"></div>
        </div>
      </div>
      <div class="relative w-64 h-64 rounded-xl overflow-hidden shadow-lg border-2 border-[var(--azul)]">
        <img 
          src="/images/servicios-finalizado.jpg" 
          alt="Espacio finalizado" 
          class="w-full h-full object-cover"
        />
      </div>
    </div>

    <!-- Lista de servicios con iconos -->
    <div class="mt-16 grid sm:grid-cols-2 lg:grid-cols-4 gap-6">
      {[
        { name: "Análisis de factibilidad", icon: "📊" },
        { name: "Anteproyecto, Diseño & Planos", icon: "📐" },
        { name: "Presupuestos detallados", icon: "$" },
        { name: "Gestión de permisos", icon: "📋" },
        { name: "Gestión BIM", icon: "🏗️" },
        { name: "Construcción & Remodelación", icon: "👷" },
        { name: "Gerencia de proyectos", icon: "⚙️" },
        { name: "Avalúos inmobiliarios", icon: "📈" }
      ].map((service) => (
        <div 
          key={service.name} 
          class="rounded-2xl border border-slate-200 p-5 bg-white hover:border-[var(--azul)] hover:shadow-md transition-all duration-300 group cursor-pointer"
        >
          <div class="flex items-center justify-center h-10 w-10 rounded-full bg-[var(--azul)]/10 text-[var(--azul)] group-hover:bg-[var(--azul)] group-hover:text-white transition-colors duration-300 mb-3">
            <span class="text-lg">{service.icon}</span>
          </div>
          <p class="text-center font-medium text-slate-800">{service.name}</p>
        </div>
      ))}
    </div>

   <!-- Sostenibilidad -->
<div class="mt-16">
  <h3 class="text-2xl font-bold text-[var(--navy)] text-center">Sostenibilidad</h3>
  <p class="mt-3 text-slate-700 max-w-3xl mx-auto text-center">
    Arquimétrica se compromete en generar un impacto positivo y sostenible, alineando su propósito y valores a través de principios fundamentales que impulsen el desarrollo responsable, la igualdad, el crecimiento inclusivo y la colaboración estratégica.
  </p>

  <!-- Grid de Sostenibilidad -->
  <div class="mt-8 grid grid-cols-1 md:grid-cols-3 gap-6 max-w-5xl mx-auto">
    {/* Ambiental */}
    <div class="flex flex-col items-center p-4 bg-gray-50 rounded-xl border border-slate-200">
      <div class="w-full text-center mb-4">
        <span class="inline-block px-3 py-1 text-sm font-medium text-[var(--azul)] bg-white border border-[var(--azul)] rounded-md">Ambiental</span>
      </div>
      <div class="flex justify-center">
        <img 
          src="/images/sostenibilidad/ods-12.webp" 
          alt="ODS 12: Producción y consumo responsables" 
          class="w-24 h-24 object-contain"
        />
      </div>
    </div>

    {/* Social */}
    <div class="flex flex-col items-center p-4 bg-gray-50 rounded-xl border border-slate-200">
      <div class="w-full text-center mb-4">
        <span class="inline-block px-3 py-1 text-sm font-medium text-[var(--azul)] bg-white border border-[var(--azul)] rounded-md">Social</span>
      </div>
      <div class="flex justify-center">
        <img 
          src="/images/sostenibilidad/ods-5.webp" 
          alt="ODS 5: Igualdad de género" 
          class="w-24 h-24 object-contain"
        />
      </div>
    </div>

    {/* Gobernanza */}
    <div class="flex flex-col items-center p-4 bg-gray-50 rounded-xl border border-slate-200">
      <div class="w-full text-center mb-4">
        <span class="inline-block px-3 py-1 text-sm font-medium text-[var(--azul)] bg-white border border-[var(--azul)] rounded-md">Gobernanza</span>
      </div>
      <div class="flex justify-center">
        <img 
          src="/images/sostenibilidad/ods-17.webp" 
          alt="ODS 17: Alianzas para lograr los objetivos" 
          class="w-24 h-24 object-contain"
        />
      </div>
    </div>
  </div>

  <!-- Fila Vinculados -->
  <div class="mt-6 grid grid-cols-1 md:grid-cols-3 gap-6 max-w-5xl mx-auto">
    {/* Industria, Innovación e Infraestructura */}
    <div class="flex flex-col items-center p-4 bg-gray-50 rounded-xl border border-slate-200">
      <div class="w-full text-center mb-4">
        <span class="inline-block px-3 py-1 text-sm font-medium text-[var(--azul)] bg-white border border-[var(--azul)] rounded-md">Ambiental</span>
      </div>
      <div class="flex justify-center">
        <img 
          src="/images/sostenibilidad/ods-9.webp" 
          alt="ODS 9: Industria, innovación e infraestructura" 
          class="w-24 h-24 object-contain"
        />
      </div>
    </div>

    {/* Salud y Bienestar */}
    <div class="flex flex-col items-center p-4 bg-gray-50 rounded-xl border border-slate-200">
      <div class="w-full text-center mb-4">
        <span class="inline-block px-3 py-1 text-sm font-medium text-[var(--azul)] bg-white border border-[var(--azul)] rounded-md">Social</span>
      </div>
      <div class="flex justify-center">
        <img 
          src="/images/sostenibilidad/ods-3.webp" 
          alt="ODS 3: Salud y bienestar" 
          class="w-24 h-24 object-contain"
        />
      </div>
    </div>

    {/* Paz, Justicia e Instituciones Sólidas */}
    <div class="flex flex-col items-center p-4 bg-gray-50 rounded-xl border border-slate-200">
      <div class="w-full text-center mb-4">
        <span class="inline-block px-3 py-1 text-sm font-medium text-[var(--azul)] bg-white border border-[var(--azul)] rounded-md">Gobernanza</span>
      </div>
      <div class="flex justify-center">
        <img 
          src="/images/sostenibilidad/ods-16.webp" 
          alt="ODS 16: Paz, justicia e instituciones sólidas" 
          class="w-24 h-24 object-contain"
        />
      </div>
    </div>
  </div>
</div>
  </div>
</section>


<!-- CTA -->
<section class="container-px py-16 text-center">
<h2 class="text-3xl font-bold text-[var(--navy)]">¿Listo para iniciar?</h2>
<p class="mt-3 text-slate-700">Conversemos su idea y definamos el plan de acción.</p>
<a href="/contacto" class="btn-primary mt-6">Agendar una llamada</a>
</section>
</BaseLayout>