---
import BaseLayout from '../layouts/BaseLayout.astro'
const CTA_WHATSAPP = '50662202104' // ej. Melissa
const CTA_WHATSAPP_2 = '50688455207' // ej. Adrián
const MAIL_1 = 'melissa.delgado@arquimetrica.cr'
const MAIL_2 = 'adrian.bolanos@arquimetrica.cr'
---
<BaseLayout title="Contacto — Arquimétrica" description="Conversemos su proyecto y definamos próximos pasos.">
  <!-- Hero compacto -->
  <section class="bg-[var(--azul)] text-white">
    <div class="container-px py-14">
      <div class="max-w-3xl">
        <h1 class="text-3xl md:text-4xl font-bold leading-tight">Contacto</h1>
        <p class="mt-3 text-white/90">
          Definamos el alcance, tiempos y presupuesto de su proyecto. Responderemos con propuestas claras y accionables.
        </p>
        <div class="mt-6 flex flex-wrap gap-3">
          <a
            href={`https://wa.me/${CTA_WHATSAPP}?text=${encodeURIComponent('Hola, me interesa conversar sobre un proyecto.')}`}
            class="inline-flex items-center gap-2 rounded-xl bg-white text-[var(--navy)] px-5 py-3 font-semibold shadow hover:shadow-md transition"
            aria-label="Contactar por WhatsApp"
          >
            WhatsApp Melissa
          </a>
          <a
            href={`https://wa.me/${CTA_WHATSAPP_2}?text=${encodeURIComponent('Hola, me interesa conversar sobre un proyecto.')}`}
            class="inline-flex items-center gap-2 rounded-xl border border-white/30 px-5 py-3 font-semibold hover:bg-white/10 transition"
            aria-label="Contactar por WhatsApp"
          >
            WhatsApp Adrián
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- Información de contacto -->
  <section class="container-px py-14">
    <div class="grid md:grid-cols-3 gap-6">
      <div class="rounded-2xl border border-slate-200 p-6">
        <h2 class="text-lg font-semibold text-[var(--navy)]">Correo</h2>
        <div class="mt-3 space-y-2">
          <a href={`mailto:${MAIL_1}`} class="block text-[var(--navy)] hover:underline">{MAIL_1}</a>
          <a href={`mailto:${MAIL_2}`} class="block text-[var(--navy)] hover:underline">{MAIL_2}</a>
        </div>
      </div>

      <div class="rounded-2xl border border-slate-200 p-6">
        <h2 class="text-lg font-semibold text-[var(--navy)]">Teléfonos</h2>
        <div class="mt-3 space-y-2">
          <a href="tel:+50662202104" class="block hover:underline">+506 6220-2104 (Melissa)</a>
          <a href="tel:+50688455207" class="block hover:underline">+506 8845-5207 (Adrián)</a>
        </div>
      </div>

      <div class="rounded-2xl border border-slate-200 p-6">
        <h2 class="text-lg font-semibold text-[var(--navy)]">Disponibilidad</h2>
        <ul class="mt-3 text-slate-700 space-y-1">
          <li>Lun–Vie: 8:30 a.m. – 6:00 p.m.</li>
          <li>Reuniones virtuales o presenciales</li>
          <li>Respuesta en &lt; 24 h hábiles</li>
        </ul>
      </div>
    </div>

    <!-- CTA final -->
    <div class="mt-10 rounded-2xl border border-dashed border-slate-300 p-6 md:p-8 text-center">
      <p class="text-slate-700">
        ¿Tiene un RFP o alcance preliminar? Envíelo por correo y preparamos una propuesta con tiempos y costos.
      </p>
      <div class="mt-4 flex flex-wrap justify-center gap-3">
        <a href={`mailto:${MAIL_1}`} class="inline-flex items-center gap-2 rounded-xl bg-[var(--navy)] text-white px-5 py-3 font-semibold hover:opacity-90 transition">
          Escribir a Melissa
        </a>
        <a href={`mailto:${MAIL_2}`} class="inline-flex items-center gap-2 rounded-xl border border-[var(--navy)] text-[var(--navy)] px-5 py-3 font-semibold hover:bg-[var(--navy)]/5 transition">
          Escribir a Adrián
        </a>
      </div>
    </div>
  </section>
</BaseLayout>
