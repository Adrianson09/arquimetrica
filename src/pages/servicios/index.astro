---
import BaseLayout from '../../layouts/BaseLayout.astro'
---

<BaseLayout title="Servicios — Arquimétrica" description="Proceso integral llave en mano: del papel a la realidad.">
  <!-- Hero Section -->
  <section class="relative bg-[var(--azul)] text-white py-20">
    <div class="container-px text-center">
      <h1 class="text-4xl md:text-5xl font-bold mb-4">Servicios</h1>
      <p class="text-xl max-w-3xl mx-auto leading-relaxed">
        Ofrecemos proceso integral llave en mano — del papel a la realidad.
      </p>
      <p class="mt-4 max-w-2xl mx-auto">
        Nos adaptamos a la necesidad de cada cliente y su proyecto, ya sea como aliado estratégico integral o como especialista en alguna fase del proceso.
      </p>
    </div>
  </section>

  <!-- Proceso Visual: Fases del Proyecto -->
  <section class="py-16 bg-white">
    <div class="container-px">
      <h2 class="text-3xl font-bold text-[var(--navy)] text-center mb-12">Nuestro Proceso</h2>
      <div class="flex flex-col md:flex-row items-center justify-center gap-8 md:gap-12">
        {/* Fase 1: Análisis */}
        <div class="text-center">
          <div class="relative w-48 h-48 mx-auto rounded-xl overflow-hidden shadow-lg border-2 border-[var(--azul)]">
            <img 
              src="/images/servicios/fase1-analisis.jpg" 
              alt="Análisis de factibilidad y planificación" 
              class="w-full h-full object-cover"
            />
          </div>
          <h3 class="mt-4 text-xl font-semibold text-[var(--navy)]">Análisis & Planificación</h3>
          <p class="text-slate-600 mt-2">Definimos viabilidad, presupuesto y cronograma.</p>
        </div>

        {/* Separador */}
        <div class="hidden md:flex items-center">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8 text-[var(--azul)]" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
          </svg>
        </div>

        {/* Fase 2: Diseño */}
        <div class="text-center">
          <div class="relative w-48 h-48 mx-auto rounded-xl overflow-hidden shadow-lg border-2 border-[var(--azul)]">
            <img 
              src="/images/servicios/fase2-diseno.jpg" 
              alt="Diseño arquitectónico y planos" 
              class="w-full h-full object-cover"
            />
          </div>
          <h3 class="mt-4 text-xl font-semibold text-[var(--navy)]">Diseño & Documentación</h3>
          <p class="text-slate-600 mt-2">Planos, renders, especificaciones técnicas.</p>
        </div>

        {/* Separador */}
        <div class="hidden md:flex items-center">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8 text-[var(--azul)]" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
          </svg>
        </div>

        {/* Fase 3: Ejecución */}
        <div class="text-center">
          <div class="relative w-48 h-48 mx-auto rounded-xl overflow-hidden shadow-lg border-2 border-[var(--azul)]">
            <img 
              src="/images/servicios/fase3-ejecucion.jpg" 
              alt="Construcción y supervisión" 
              class="w-full h-full object-cover"
            />
          </div>
          <h3 class="mt-4 text-xl font-semibold text-[var(--navy)]">Ejecución & Control</h3>
          <p class="text-slate-600 mt-2">Construcción, inspección y entrega final.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Detalle de Servicios por Categoría -->
  <section class="py-16 bg-[var(--light)]">
    <div class="container-px">
      <h2 class="text-3xl font-bold text-[var(--navy)] text-center mb-12">Nuestros Servicios</h2>

      <!-- Categoría 1: Pre-construcción -->
      <div class="mb-16">
        <h3 class="text-2xl font-bold text-[var(--navy)] mb-8 text-center md:text-left">Pre-construcción</h3>
        <div class="grid md:grid-cols-3 gap-8">
          {[
            {
              name: "Análisis de Factibilidad",
              desc: "Evaluamos técnicamente y económicamente la viabilidad de su proyecto, considerando normativas, costos, tiempos y retorno de inversión.",
              icon: "📊"
            },
            {
              name: "Anteproyecto, Diseño & Planos",
              desc: "Desarrollamos propuestas arquitectónicas creativas y funcionales, con planos constructivos detallados y renders 3D para visualizar el resultado final.",
              icon: "📐"
            },
            {
              name: "Presupuestos Detallados",
              desc: "Elaboramos presupuestos precisos por ítems, con desglose de materiales, mano de obra y tiempos, para una gestión financiera eficiente.",
              icon: "$"
            }
          ].map((service) => (
            <div 
              key={service.name} 
              class="rounded-2xl border border-slate-200 p-6 bg-white hover:border-[var(--azul)] hover:shadow-lg transition-all duration-300 group"
            >
              <div class="flex items-center justify-center h-12 w-12 rounded-full bg-[var(--azul)]/10 text-[var(--azul)] group-hover:bg-[var(--azul)] group-hover:text-white transition-colors duration-300 mb-4 mx-auto">
                <span class="text-xl">{service.icon}</span>
              </div>
              <h4 class="text-lg font-semibold text-[var(--navy)] text-center mb-3">{service.name}</h4>
              <p class="text-slate-700 text-sm leading-relaxed text-center">{service.desc}</p>
            </div>
          ))}
        </div>
      </div>

      <!-- Categoría 2: Gestión & Ejecución -->
      <div class="mb-16">
        <h3 class="text-2xl font-bold text-[var(--navy)] mb-8 text-center md:text-left">Gestión & Ejecución</h3>
        <div class="grid md:grid-cols-3 gap-8">
          {[
            {
              name: "Gestión de Permisos",
              desc: "Gestionamos y tramitamos todos los permisos ante municipalidades y entidades reguladoras, asegurando el cumplimiento normativo.",
              icon: "📋"
            },
            {
              name: "Gestión BIM",
              desc: "Implementamos metodología BIM para coordinar disciplinas, detectar interferencias y optimizar recursos desde la etapa de diseño.",
              icon: "🏗️"
            },
            {
              name: "Construcción & Remodelación",
              desc: "Ejecutamos obras nuevas o remodelaciones con supervisión técnica constante, garantizando calidad, plazos y presupuesto.",
              icon: "👷"
            }
          ].map((service) => (
            <div 
              key={service.name} 
              class="rounded-2xl border border-slate-200 p-6 bg-white hover:border-[var(--azul)] hover:shadow-lg transition-all duration-300 group"
            >
              <div class="flex items-center justify-center h-12 w-12 rounded-full bg-[var(--azul)]/10 text-[var(--azul)] group-hover:bg-[var(--azul)] group-hover:text-white transition-colors duration-300 mb-4 mx-auto">
                <span class="text-xl">{service.icon}</span>
              </div>
              <h4 class="text-lg font-semibold text-[var(--navy)] text-center mb-3">{service.name}</h4>
              <p class="text-slate-700 text-sm leading-relaxed text-center">{service.desc}</p>
            </div>
          ))}
        </div>
      </div>

      <!-- Categoría 3: Valor Agregado -->
      <div class="mb-16">
        <h3 class="text-2xl font-bold text-[var(--navy)] mb-8 text-center md:text-left">Valor Agregado</h3>
        <div class="grid md:grid-cols-2 gap-8">
          {[
            {
              name: "Gerencia de Proyectos",
              desc: "Dirigimos integralmente su proyecto con enfoque estratégico, controlando alcance, tiempo, costo, calidad, riesgos y comunicación.",
              icon: "⚙️"
            },
            {
              name: "Avalúos Inmobiliarios",
              desc: "Realizamos avalúos técnicos para compra-venta, herencias, seguros o trámites legales, basados en metodologías reconocidas.",
              icon: "📈"
            }
          ].map((service) => (
            <div 
              key={service.name} 
              class="rounded-2xl border border-slate-200 p-6 bg-white hover:border-[var(--azul)] hover:shadow-lg transition-all duration-300 group"
            >
              <div class="flex items-center justify-center h-12 w-12 rounded-full bg-[var(--azul)]/10 text-[var(--azul)] group-hover:bg-[var(--azul)] group-hover:text-white transition-colors duration-300 mb-4 mx-auto">
                <span class="text-xl">{service.icon}</span>
              </div>
              <h4 class="text-lg font-semibold text-[var(--navy)] text-center mb-3">{service.name}</h4>
              <p class="text-slate-700 text-sm leading-relaxed text-center">{service.desc}</p>
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>

  <!-- CTA Final -->
  <section class="py-16 bg-white text-center">
    <div class="container-px">
      <h2 class="text-3xl font-bold text-[var(--navy)] mb-4">¿Listo para comenzar su proyecto?</h2>
      <p class="text-slate-700 max-w-2xl mx-auto mb-8">
        Cuéntenos su idea y definamos juntos el plan de acción. Ofrecemos asesoría inicial sin costo.
      </p>
      <a 
        href="/contacto" 
        class="inline-block rounded-xl bg-[var(--azul)] px-8 py-3 text-white font-semibold hover:bg-[var(--azul-oscuro)] transition-all duration-300 shadow-md hover:shadow-lg"
      >
        Agendar una llamada
      </a>
    </div>
  </section>
</BaseLayout>